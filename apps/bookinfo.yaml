# Generated by scripts/sync-bookinfo-to-platform-apps.sh in tomsolem/infra-demo.
# Do not edit directly — re-run the script to update.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bookinfo
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/tomsolemdemo/platform-apps.git
    targetRevision: main
    path: manifests/bookinfo
    directory:
      recurse: false
  destination:
    server: https://kubernetes.default.svc
    namespace: bookinfo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
